#include "main.h"

/**
 *_strlen - function cont a charachter
 *Description : function return the lenght of somthing
 *@s : somthing parameter to cont
 *Return: Always 0 (success)
 */

int _strlen(char *s)
{
	int l;

	l = strlen(s);

	return (l);
}

